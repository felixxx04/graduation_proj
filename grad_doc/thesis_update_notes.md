# 论文修改记录 — 基于推荐系统架构改进

> 保存日期：2026-05-10
> 参考论文：thesis_updated_v2.docx

---

## 背景

推荐系统经过以下改进，论文中相关描述需要同步更新：

1. 新增第零层：临床知识路由层（KnowledgeRouter L1→L2→L3 确定性路由）
2. 新增患者口语增强层（PatientInputEnhancer 三层fallback）
3. SafetyFilter 从"硬排除优先"调整为"标记审核"
4. 疾病覆盖从 204→1,295 种中文疾病，适应症覆盖率 12.8%→92.5%
5. 新增医生审核模块（ReviewPanel + review_log）
6. 新增反馈学习器（FeedbackLearner 自动降权）

---

## 一、第 1 章 · 摘要/Abstract（必须更新）

### 中文摘要 [P17]

**当前段：** "在核心算法层面，系统设计了三层推荐架构：第一层为安全过滤层..."

**需改为：**

"在核心算法层面，系统设计了四层推荐架构：第零层为临床知识路由层（KnowledgeRouter），通过L1口语标准化→L2疾病归类→L3适应症映射→L4药品召回的四级确定性路由，解决中文疾病名到英文药品适应症之间的语义鸿沟；第一层为安全过滤层（SafetyFilter），保留12条绝对安全红线的硬排除规则，将相对禁忌、超说明书用药等9类情形由系统自动排除改为标记待审核；第二层为规则标记层（RuleMarker），对候选药物附加临床警告；第三层为DeepFM个性化排序层。系统还集成了患者口语增强模块（PatientInputEnhancer），通过精确匹配→关键词匹配→症状组合推理的三层fallback策略处理患者非标准化口语输入。推荐结果经医生审核确认后，反馈学习器（FeedbackLearner）自动调整后续推荐权重，形成持续优化的闭环。"

**当前段：** "实验结果表明，系统在设置合理隐私预算（ε=0.1）的条件下..."

**此处无需改动，实验数据不变。**

### 英文摘要 [P25]

同步更新中文摘要的修改内容。

---

## 二、第 3 章 · 系统需求分析与设计（必须更新）

### 3.X 系统架构 [P172]

**当前架构图描述为三层**，需改为四层架构：

```
患者输入（口语化疾病/症状）
       ↓
┌──────────────────────────┐
│ 新增 · 第零层：临床知识路由层    │
│ L1 标准化：口语→医学术语         │
│ L2 归类：疾病→身体系统+病因      │
│ L3 适应症映射：系统+病因→药类    │
│ L4 药品召回：药类→具体药品       │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 第一层：安全标记层（调整后）     │
│ 绝对禁忌/过敏→硬排除（12条不变） │
│ 其他→标记待审核（9条可审核）     │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 第二层：DeepFM 排序            │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 医生审核 + 反馈闭环            │
└──────────────────────────┘
```

### 3.X 系统模块 [P176]

**当前列了5个核心模块**，需新增：

- **临床知识路由模块（KnowledgeRouter）**：四级确定性路由，将患者口语化疾病名精准对接到正确药品类别，从根源解决"推荐错药"问题。包含1,495条L2疾病分类规则和200条L3药类路由规则。
- **患者输入增强模块（PatientInputEnhancer）**：三层fallback策略（精确匹配→关键词匹配→症状组合推理），处理患者非标准化口语表达。
- **医生审核模块（ReviewPanel）**：系统推荐2-4个候选药物，医生进行确认/修改/拒绝审核决策。审核日志（review_log）记录所有决策供后续分析。
- **反馈学习模块（FeedbackLearner）**：从医生审核决策中自动学习，对反复被拒绝的疾病→药品类别配对施加渐进式惩罚（1次拒绝=0.7x，2次=0.5x，3次+=0.3x），确认操作则解除惩罚。

### 3.X 疾病覆盖能力

**新增描述：**

系统内置1,295个中文疾病名到英文标准适应症的映射，覆盖15个身体系统（心血管、呼吸、消化等），支持1,815种药品的用药推荐。通过DeepSeek批量翻译，适应症L2路由覆盖率达92.5%，患者口语表含154条常用口语词。

---

## 三、第 4 章 · 核心算法设计与实现（必须更新）

### 4.0 新增：临床知识路由层 [在P202之前插入新节]

**4.0.1 路由层设计理念**

临床知识路由层是四层推荐架构的新增第零层，核心目标是解决中文疾病名到英文药品适应症之间的语义鸿沟。原有方案中，疾病映射器将中文疾病名转换为英文适应症后，直接在全量1,815种药品中匹配适应症，缺乏中间的疾病→药类路由环节。当非标准疾病名被路由到错误的代理词时（如"肠炎"→代理"腹泻"→推荐止泻药而非抗感染药），会导致推荐错误。

知识路由层通过四级确定性路由机制，在疾病名和药品之间建立了明确的多层路由网络：

**L1 口语标准化**：将患者的非标准口语化中文表达（如"拉肚子"、"嗓子发炎"）标准化为标准英文医学术语（如"diarrhea"、"pharyngitis"）。路由表包含154条口语→标准术语映射。

**L2 疾病归类**：将标准化疾病名归类到身体系统（14个系统）和病因分类（23种病因类型）。例如"腹泻"归类为消化系统·感染性，"感冒"归类为呼吸系统·病毒性。路由表包含1,495条疾病归类记录。

**L3 适应症映射**：将身体系统+病因组合映射到对应的ATC（Anatomical Therapeutic Chemical）药品治疗类别。例如"消化系统·感染性"→肠道抗感染药，"呼吸系统·病毒性"→镇咳祛痰药（不含抗生素）。路由表包含200条L3路由规则。

**L4 药品召回**：在L3确定的药品类别范围内，召回该类别中的所有候选药物供后续安全过滤和排序。这一层由推荐预测器（predictor.py）完成。

**4.0.2 路由算法实现**

```python
def route(chinese_disease, confidence):
    # L1: 口语→标准术语
    standard_terms = l1_map.get(chinese_disease, fallback)
    # L2: 标准术语→身体系统+病因
    category = l2_map.get(standard_terms[0])
    # L3: 身体系统+病因→ATC药品类别
    atc_info = l3_map.get(f"{body_system}_{etiology}")
    return {atc_codes, drug_classes, routing_path}
```

路由是确定性的——同一疾病每次路由结果一致，无机器学习随机性。每个推荐结果都可以回溯到完整的路由路径（如"推荐Rifaximin是因为'拉肚子'→腹泻→消化系统·感染性→肠道抗感染药→Rifaximin是该类代表药"），保证了推荐的可解释性。

**4.0.3 患者口语增强**

口语增强模块采用三层fallback策略处理患者非标准化输入：第一层精确匹配口语映射表中的完整短语（如"感冒"→"common cold"）；第二层关键词匹配提取输入中的医学关键词（如"喉咙不舒服"→提取"喉咙"→"pharyngitis"）；第三层症状组合推理将多症状组合推断为最可能的疾病（如"头痛+发烧+肌肉酸痛"→流感样疾病，信心级别低，强制医生审核）。当三层均失败时，降级为症状级别推荐并标记为低置信度。

### 4.1 三层推荐安全架构 [P203-P210]

**4.1.1 安全过滤层 [P207]**

**当前：** "安全过滤层实现了17类排除规则"

**需改为：**

"安全过滤层的设计原则从'系统替患者决定'调整为'系统为医生标记，医生来做最终判断'。系统新增了SafetyLevel枚举（SAFE/WARNING/OFF_LABEL/UNVERIFIED/EXCLUDED），将原有的17类规则划分为硬排除（12条，不可审核）和软标记（9条，医生可审核）：

**硬排除规则（12条，不变）：** 1.过敏冲突 2.绝对禁忌症 3.致命药物交互（MAOI+SSRI等）4.妊娠X级 5.儿科禁忌 6.哺乳期L5级 7.草药补充剂治感染 8.糖皮质激素加重真菌感染 9.免疫抑制剂加重感染 10.IBD药物用于感染性肠炎（免疫抑制剂危险）11.抗生素治病毒感染（延长排毒期）12.PAH专用药误用于普通高血压。

**软标记规则（9条，原为排除，现改为标记待审核）：** 1.PPI用于胆囊病 2.抗生素用于尿路结石 3.降糖药用于结石 4.苯二氮卓用于胆囊病 5.青光眼药用于白内障 6.苯二氮卓用于OCD 7.促尿酸排泄药用于肠炎 8.抗生素用于真菌感染 9.无精确适应症匹配（off_label）。

这一调整的核心优势在于：绝对安全红线保持不变，但原被过度拦截的药物现在以'标记待审'方式呈现在医生面前，医生可基于临床经验做最终决策。"

**4.1.2 规则标记层 [P210] 改动较小，补充新字段说明**

**4.1.3 个性化排序层 补充药类评分和反馈惩罚**

"在DeepFM排序之前，系统先通过知识路由层确定药物类别范围，对候选药物施加药类匹配评分（+0.25分）和对错误药类药物的惩罚（×0.3，如病毒性疾病中的抗生素）。排序时采用安全级别优先级排序（safe=3 > warning=2 > off_label/unverified=1）。此外，反馈学习器对历史被医生拒绝的疾病→药物类别配对施加渐进式惩罚（1次拒绝×0.7，2次×0.5，3次+×0.3），使系统能从审核反馈中自动学习优化。"

### 4.4 推荐流程集成 [P232]

**4.4.1 三层推荐流程设计 [P234]**

需更新流程图，增加以下步骤：
0. 患者口语增强（PatientInputEnhancer）
1. 知识路由（KnowledgeRouter L1→L2→L3）
2. 安全过滤（调整后的SafetyFilter）
3. 规则标记（RuleMarker）
4. DeepFM排序（含药类评分+反馈惩罚）
5. 医生审核（ReviewPanel）
6. 反馈记录（review_log→FeedbackLearner）

**4.4.2 疾病映射与特征预处理 [P238]**

**当前：** "DiseaseMapper通过SEMANTIC_VOCAB_MAP解决...204种疾病"

**需改为：**

"疾病映射与特征预处理由三个模块协同完成：DiseaseMapper负责中文→英文疾病名的基础映射（1,295条），PathInputEnhancer负责患者口语输入的标准化增强（154条口语词+30条症状组合规则），KnowledgeRouter负责疾病归类与药品类别路由（1,495条L2分类+200条L3路由）。三个模块形成从输入端到排序端的完整知识路由链，将原有的44.6%直接匹配率提升至92.5%。"

### 新增 4.5：审核反馈闭环

"4.5.1 审核流程设计

系统推荐2-4个候选药物后，医生进入审核环节，可进行三种操作：✅确认推荐（记录正面信号）、✏️修改选择（从候选列表中另选药物）、❌拒绝推荐（标记为不适用）。审核决策通过ReviewPanel前端组件收集，经/api/review/log接口写入MySQL review_log表。

4.5.2 反馈学习机制

反馈学习器（FeedbackLearner）从review_log中读取审核决策，自动构建疾病→药品类别的惩罚权重图。当某药品类别被医生反复拒绝（如腹泻+PPI），系统自动对该配对施加渐进式惩罚，后续推荐中该药类候选药物得分乘以相应惩罚系数。当医生确认了曾被拒绝的药类时，惩罚系数逐步解除。惩罚数据持久化为JSON文件，可供后续分析。"

---

## 四、第 5 章 · 系统实现与测试（建议更新）

### 5.X 推荐页面 [P259]

**当前描述缺少以下功能，需补充：**

- **安全级别标签（SafetyBadge）**：每个推荐药物旁显示安全级别：绿色"安全"、黄色"需谨慎"、橙色"超说明书"、紫色"待验证"
- **推荐路径展示**：显示"推荐路径：疾病匹配→[匹配的疾病]→[药品类别]"的可追溯路由信息
- **证据等级标签**：显示"说明书内"/"超说明书"用药依据
- **医生审核面板（ReviewPanel）**：推荐列表下方显示确认/修改/拒绝三个操作按钮

### 5.X 测试 [P298]

**补充测试数据：**

"系统共实现了232个自动化测试用例，包括知识路由引擎测试（10个测试用例）、患者输入增强测试（36个测试用例）、204种疾病回归测试（186个测试用例），所有测试均已通过。此外，通过DeepSeek临床药学专家验证，系统推荐适用率（APPROPRIATE）达61.1%，部分适用率（PARTIALLY）37.5%，不适用率（INAPPROPRIATE）仅1.4%。经过架构改进后，疾病适应症覆盖率达92.5%。"

---

## 五、第 6 章 · 总结与展望 [P308-P310]

### 总结 [P308]

**当前：** "核心创新为三层推荐安全架构"

**需改为：**

"核心创新为四层推荐架构：第零层临床知识路由层通过四级确定性路由精准对接疾病与药品类别，解决了中文疾病名到英文适应症之间的语义鸿沟；第一层安全标记层将过度拦截调整为标记审核，在保障绝对安全底线的同时扩大了候选药物范围；第二层规则标记层提供临床警告；第三层DeepFM个性化排序层融合药类评分和反馈学习。系统还实现了患者口语增强和医生审核反馈闭环，使系统支持1,295种中文疾病输入，适应症路由覆盖率达92.5%，并通过232个自动化测试和DeepSeek临床药学验证。"

### 展望

新增一段关于持续优化方向：

"未来工作可在以下方面继续推进：(1)扩充低频适应症（120个罕见病）的路由覆盖至100%；(2)利用审核反馈数据持续优化疾病→药品类别的路由权重；(3)将安全性数据（禁忌症数量、交互严重度）编码为DeepFM模型特征，实现更精细的个性化排序。"

---

## 文件修改概览

| 章节 | 修改程度 | 涉及段落 |
|------|---------|---------|
| 第1章 摘要 | 中等 | P17-P18, P25 |
| 第3章 系统设计 | **大幅** | P172, P176, 架构图 |
| 第4章 算法实现 | **大幅** | 新增4.0节, P207-P208, P237-P239, 新增4.5节 |
| 第5章 系统实现 | 中等 | P259, P288, P298 |
| 第6章 总结展望 | 中等 | P308-P310 |
| 英文摘要 | 同步 | P25 |
